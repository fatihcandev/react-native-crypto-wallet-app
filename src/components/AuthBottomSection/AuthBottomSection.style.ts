import { StyleSheet } from 'react-native';
import { spacing } from 'styles';

export default StyleSheet.create({
  container: {
    alignItems: 'center',
  },
  textContainer: {
    marginTop: spacing.mV,
  },
  lightText: {
    marginRight: spacing.sH,
  },
});
