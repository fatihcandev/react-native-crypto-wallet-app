import { StyleSheet } from 'react-native';
import { spacing } from 'styles';

export default StyleSheet.create({
  container: {
    paddingHorizontal: spacing.sH,
  },
  coinLabel: {
    marginLeft: spacing.lH,
  },
});
