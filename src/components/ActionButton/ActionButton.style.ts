import { StyleSheet } from 'react-native';
import { spacing } from 'styles';

export default StyleSheet.create({
  label: {
    marginLeft: spacing.sH,
  },
  info: {
    marginRight: spacing.sH,
  },
});
