import React from 'react';

import { Background, StyledText } from 'components';

const ScanBackSide = () => {
  return (
    <Background>
      <StyledText variant="h1">ScanBackSide</StyledText>
    </Background>
  );
};

export default ScanBackSide;
