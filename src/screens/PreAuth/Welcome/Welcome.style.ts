import { StyleSheet } from 'react-native';
import { spacing } from 'styles';

export default StyleSheet.create({
  container: {
    paddingBottom: spacing.xlV,
  },
  logo: {
    marginTop: spacing['6xlV'],
    marginBottom: spacing.xlV,
  },
});
